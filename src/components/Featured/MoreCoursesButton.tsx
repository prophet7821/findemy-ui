// @ts-ignore
import React from 'react'
import {Button,styled} from '@mui/material'

const MoreCoursesButton = styled(Button)({
    border: "2px solid #000",
    borderRadius: "none",
    color:'black',
    "&:hover": {
        border: "2px solid #000",
        borderRadius: "none",
  },
});

export default MoreCoursesButton