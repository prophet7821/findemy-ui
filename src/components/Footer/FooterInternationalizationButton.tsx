import React from 'react'
import {Button,styled} from '@mui/material'

const FooterInternationalizationalButton = styled(Button)({
  border: "2px solid #fff",
  borderRadius: 0,
  color:'white',
  "&:hover": {
    border: "2px solid #fff",
    borderRadius: 0,
  },
});

export default FooterInternationalizationalButton